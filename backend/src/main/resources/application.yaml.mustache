# Spring Boot Configuration
# Generated by Genesis3

spring:
  application:
    name: {{{projectName}}}-api

server:
  port: ${SERVER_PORT:8080}

app:
  env:
    host: ${APP_ENV_HOST:http://localhost:8080}
    ui: ${APP_ENV_UI:http://localhost:3000}
    buildTool: {{backendModule.buildTool}}

logging:
  level:
    {{{javaPackageFull}}}: ${LOG_LEVEL_APP:INFO}
    org.springframework: ${LOG_LEVEL_SPRING:INFO}
    org.springframework.web: ${LOG_LEVEL_WEB:INFO}
