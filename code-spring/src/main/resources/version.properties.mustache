# Version and Build Metadata for {{{projectName}}}
# This file is typically overwritten by CI/CD pipelines during deployment
# The properties below are placeholders for local development

# GitHub Actions metadata (populated during CI/CD builds)
gh.build=local
gh.attempt=1
gh.action=local-development
gh.modifiedDate={{generatedAt}}
gh.sha=local

# To override these values during build, CI/CD should create this file with actual values
# Example from GitHub Actions:
# gh.build={{{githubVarsOpen}}} github.run_number {{{githubVarsClose}}}
# gh.attempt={{{githubVarsOpen}}} github.run_attempt {{{githubVarsClose}}}
# gh.action={{{githubVarsOpen}}} github.workflow {{{githubVarsClose}}}
# gh.modifiedDate=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
# gh.sha={{{githubVarsOpen}}} github.sha {{{githubVarsClose}}}
